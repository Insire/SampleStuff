<UserControl x:Class="MVVMHierarchiesDemo.Views.DisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Width="100" Text="Name" />
                <TextBlock Text="{Binding MainViewModel.SelectedPerson.Name}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock Width="100" Text="Age" />
                <TextBlock Text="{Binding MainViewModel.SelectedPerson.Age}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock Width="100" Text="Birthday" />
                <TextBlock Text="{Binding MainViewModel.SelectedPerson.Birthday}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock Width="100" Text="Clicker Count" />
                <TextBlock Text="{Binding MainViewModel.SelectedPerson.ClickerCount}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
